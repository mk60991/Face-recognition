NOTE:
Before run the code must check the PATH of specified folder

1. first run :face_dataset.py : to take photo from camera for training. when run it require "enter your id:"

2. second run : training.py : to trained dataset which is stored in dataSet folder


3. finally : face_recognition.py : to detect faces
